"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.executeBuffer = exports.execute = void 0;
const spawnVBS_1 = require("./spawnVBS");
const cscript_1 = require("./cscript");
const path_1 = require("path");
const execute = (vbsFile, args) => __awaiter(void 0, void 0, void 0, function* () {
    const nologo = "//NOLOGO";
    const engine = "//E:VBScript";
    const command = (0, cscript_1.cscript)();
    const file = (0, path_1.resolve)(vbsFile);
    const result = yield (0, spawnVBS_1.spawnVBS)({
        command,
        args: [nologo, engine, file, ...args],
    });
    return result;
});
exports.execute = execute;
const executeBuffer = (vbsFile, args) => __awaiter(void 0, void 0, void 0, function* () {
    const nologo = "//NOLOGO";
    const engine = "//E:VBScript";
    const command = (0, cscript_1.cscript)();
    const file = (0, path_1.resolve)(vbsFile);
    const result = yield (0, spawnVBS_1.spawnVBSbuffer)({
        command,
        args: [nologo, engine, file, ...args],
    });
    return result;
});
exports.executeBuffer = executeBuffer;
